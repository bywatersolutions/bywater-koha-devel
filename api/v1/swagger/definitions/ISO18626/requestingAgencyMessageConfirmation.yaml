---
type: object
properties:
  requestingAgencyMessageConfirmation:
    type: object
    properties:
      confirmationHeader:
        $ref: './confirmation/confirmationHeader.yaml'
      action:
        $ref: './types/action.yaml'
      errorData:
        type: array
        items:
          $ref: './confirmation/errorData.yaml'
    required:
      - confirmationHeader
    additionalProperties: false
required:
  - requestingAgencyMessageConfirmation
additionalProperties: false
